# Security

## Supported version

当前支持 v0.1.x。安全问题请通过 GitHub Security Advisory 私下报告，不要在公开 Issue 中附带设备标识、日志中的个人信息或签名材料。

## 当前安全边界

- 游戏静态服务只绑定 `127.0.0.1`，只接受有界的 GET/HEAD 请求，并校验规范化路径。
- Android 禁止明文外网流量；动态内容仅允许指定 GitHub HTTPS 来源。
- 更新包必须同时通过版本、声明大小、64 MiB 总量、SHA-256、文件数量、单文件大小、解压总量及 ZIP 路径穿越校验。
- 下载先进入 staging 目录，完整校验后才原子切换，并在下一次启动生效。
- 不向网页暴露 JavaScript 原生桥；脚本和资源受本地 CSP 限制。
- Android 备份和设备迁移备份关闭；签名私钥及属性文件不进入仓库。

这些措施和自动化扫描只能说明已测试范围内未发现问题，不构成“绝对无漏洞”的承诺。
