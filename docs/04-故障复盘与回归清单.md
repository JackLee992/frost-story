# 冰霜物语故障复盘与回归清单

> 本文记录首个可玩版中已遇到的用户可见问题、真正根因、修复方式和防再发测试。新游戏应直接复用末尾的发布门禁。

## 处理原则

- 现象和根因分开记录；“看起来像网格”不等于根因就在网格层。
- 状态改动先校验后写入，失败分支必须零写入。
- UI 只在命令成功时关闭或局部刷新；失败只显示原因。
- 渲染引擎对象不存放业务自定义字段，跨帧信息放进 `WeakMap`。
- 每个修复至少有一个可自动重现的用例，并在 Android 真机重跑核心路径。

## 问题台账

| ID | 现象 | 根因 | 通用修复 | 回归 |
|---|---|---|---|---|
| FS-001 | Android 启动期存在 Gecko 子进程重复初始化风险 | `Application.onCreate()` 会进入 GPU/内容/崩溃辅助进程 | `GeckoRuntime` 用主进程 Activity 首次访问的同步惰性单例 | Android 启动、前后台、重启 |
| FS-002 | 重启后像“存档丢失” | 本地 HTTP 随机端口改变 origin，`localStorage` 按 origin 隔离 | 回环服务器固定优先使用 `127.0.0.1:17843` | E2E 重载存档；真机强停/重启 |
| FS-003 | 全屏后 HUD、棋盘或底部按钮被挖孔/手势区遮挡 | 沉浸式全屏没有约束核心内容 | 原生层读取 `DisplayCutout` 与强制手势边距，换算 CSS px，全界面共用安全矩形 | 四方向 E2E 边界断言；真机截图 |
| FS-004 | “爽玩”按钮遮挡内容，Android 拖动后偶发不保存 | 位置固定；某些触摸序列以 `pointercancel` 结束 | 可拖动、安全区限幅、左右吸边；移动中防抖保存，`up/cancel` 共用收尾 | E2E 吸边、不压导航、重载持久化 |
| FS-005 | 屏幕出现多条贯穿全屏的蓝色竖条 | 动画把起点写入 Pixi 内部字段 `_sx/_sy`，破坏 Transform 矩阵缓存，节点高度膨胀到上万像素 | 动画起点放闭包；所有棋子元数据迁移到核心包的 `WeakMap` store | E2E 断言节点 bounds 在 1–120 px，无业务 `_...` 字段 |
| FS-006 | 合并后旧节点残留，长时游玩会叠加隐形对象 | `tween` 销毁回调误传第四参数 | 销毁函数传入 `onDone`，完成时删除 `WeakMap` 元数据 | E2E 断言场景子节点数等于状态节点数 |
| FS-007 | 合并成功但图像偶发仍是上一阶 | 纹理缓存 key 只含家族，未含阶级 | key 同时含 `family + tier` | 剧情内教学真实拖拽 E2E |
| FS-008 | 点商店条目时列表反复收起/显示，然后提示金币不足 | 无论交易成败都再调 `_shop()`，而 `modal()` 先销毁旧弹窗 | 状态命令返回 `ActionResult`；失败不重绘，成功只替换 `.m-body`并恢复滚动位置 | 6 类失败零写入；E2E 检查弹窗 identity/scrollTop |
| FS-009 | 融冰、宝箱钻石加速失败时仍关闭确认框 | UI 无条件 `closeModal()` | 确认类交互统一通过 `runActionOnce`，仅成功回调关闭 | E2E 覆盖钻石/金币不足时确认框仍在 |
| FS-010 | 霜泡外观在多次刷新后积累隐藏 Text 子节点 | `Graphics.clear()` 只清几何，不删子节点 | 更新前 `removeChildren()` 并销毁旧覆盖层 | E2E 连续 20 次 sync 后仍只有 1 个子节点 |
| FS-011 | 新手首单可随机成当前无法完成的需求 | 教学依赖随机订单 | 首次来访固定为冈特请求二阶结晶，奖励足够继续融冰；同批 NPC 去重 | 逻辑测试 + 剧情内教学 E2E |
| FS-012 | 动态更新 ZIP 可带来路径穿越、超大包或半更新 | 将网络包当作可信输入 | HTTPS 白名单、SHA-256、大小/数量/解压配额、路径校验，验证后原子切换且下次启动生效 | Android 测试/lint，内容包 SHA，真机启动 |
| FS-013 | 引入 Git submodule 后，开发元数据可被带入 APK/内容包 | 直接复制整个 submodule，其中 `.git`、CI、测试与文档均非运行时必需 | 发布只保留 `engine/src`；本地服务与 ZIP 另外拒绝任何隐藏路径段 | Kotlin 路径策略单测；检查 APK/ZIP 文件清单 |
| FS-014 | 全新 GitHub Actions checkout 在同步 Android 资源时失败 | `android/app/src/main/assets/` 是被忽略的生成目录，脚本假定父目录已存在 | 同步前显式创建目标父目录，并允许测试注入隔离目标目录 | `test/sync-android.test.js` 从不存在的多级父目录开始同步；CI 再从干净 checkout 验证 |
| FS-015 | GitHub Actions 解析 Android 插件依赖时被镜像站 502 中断 | 第三方镜像排在官方仓库前；Gradle 收到 5xx 后不会再把同一元数据请求交给后续仓库 | 官方 Google/Maven/Plugin Portal 优先，镜像只作大陆网络回退 | 静态顺序回归 6 项；干净 GitHub runner 完整构建 |
| FS-016 | 剧情数量增加后仍像功能 Demo，每句对白都有“换页”感 | UI 为每句台词销毁并重建弹窗；角色站位、镜头和上下文无法连续 | 宿主 UI 改为单一电影舞台，在同一 DOM identity 内切台词、角色焦点和逐字状态，并提供回顾/跳过 | E2E 确认切句后 modal identity 不变、回顾文本完整；真机还需检查连点与返回键 |
| FS-017 | 横向 CG 在竖屏全屏里被 `cover` 裁掉人物脸部和关键动作 | 单层背景同时承担氛围铺满和信息展示，两个目标在竖屏不可同时满足 | 通用叙事 UI 采用“模糊 `cover` 氛围层 + `contain` 完整 16:9 画幅”；操作点只相对完整画幅定位 | E2E 检查 CG 资源、三个交互点、挖孔安全区和完成态文案；手工对比 360×800 截图 |
| FS-018 | 重建仅扣材料和播对白，玩家没有“是我修好的”的操作记忆 | 节点配置只描述需求与对白，缺少完成前的交互契约及选择状态 | 宿主新增数据驱动 `tap-sequence` / `choice`；选择通过白名单命令原子写入原存档，后续对白和手记回指 | 逻辑测试验证配置/零写入/存档过滤；E2E 贯通点火三步和村厅灯选择 |
| FS-019 | 委托横条和“重燃篝火广场”主线条同时常驻，遮住合成盘 | 两个 HTML HUD 各自默认完整展开，Pixi 棋盘又不会随覆盖层重排 | 合并为 38px 双入口任务架，默认都收起；玩家一次只展开一栏，状态写入存档，前往主线/完成委托后自动收起 | E2E 断言收起高度不超过 48px、底边不越过棋盘起点、互斥展开与重载持久化 |
| FS-020 | 三个委托开局自动出现且没有来历，材料齐即可直接交付 | 订单模型只有材料与奖励，没有“请求→接受→进行中→回应”生命周期 | NPC 配置加入 `arrival/request/thanks`；新请求 `accepted:false`，必须经玩家回应；进行中不可刷新，交付后生成新的待接受请求 | 逻辑测试覆盖未接受零写入、重复接受、旧档迁移、交付补单；E2E 覆盖冈特来访与回应 |
| FS-021 | 有 BGM/SFX 文件但玩家仍感到“没有语音和音效” | 移动端自动播放限制使首次无手势启动无声；对白没有语音元数据与播放器，BGM 也不会为台词让位 | 首屏改为玩家“点亮风灯”后初始化 AudioContext；增加独立语音通道、切句停止、BGM duck、单独开关和 12 段关键离线语音 | 逻辑测试逐个读取 12 个 Opus；E2E 确认 9 个 SFX 解码且首句语音处于播放态；真机检查媒体会话与听感 |
| FS-022 | 真机点“点亮风灯”后媒体会话已启动，但画面一直停在点灯页；先解除等待后画面能转场，但首句语音仍可能被 GeckoView 拦截 | 点击处理曾等待音频初始化；解除等待后，语音又在异步解锁完成时才调用 `play()`，已经脱离用户手势 | 点击后立即进入序章；HTMLAudio 在同一次点击调用栈内直接起播，WebAudio/SFX 独立异步解锁，失败语音保留 `pendingVoice` 重试 | E2E 人为延迟 WebAudio 900 ms，要求 700 ms 内语音进度已前进且画面已转场；真机 1 秒截图进入序章，系统同时记录 48 kHz 媒体播放 |

## 竖条问题的定位过程

1. 先将 48 个网格连续描边改为弱填充/落点，视觉噪声降低，但全屏竖条仍在。
2. 依次隔离 grid、item、fx、snow 和生成器，确认竖条只属于 `itemLayer`。
3. 检查活动 DisplayObject bounds，发现新节点高度从 0 跳到约 16566 px。
4. 对照 Pixi Transform 后确认业务代码写入同名 `_sx/_sy`。
5. 用闭包保存动画起点立即消除竖条；再将所有自定义节点字段迁入 `WeakMap`，防止未来引擎版本出现新冲突。

该过程说明：图形异常应使用“分层隔离 + bounds 数值检查”，不能只靠截图猜 CSS 或网格样式。

## 交易交互契约

```text
点击按钮
  → runActionOnce 拦截重复激活
  → GameState 完成全部前置校验
      → 失败 ActionResult: 零写入 + 弹窗不动 + toast
      → 成功 ActionResult: 一次状态提交 + 局部刷新/关闭
```

已迁移的命令：商店宝箱、生成器、体力瓶、宝箱加速/开启、融冰、订单刷新和订单交付。

## 当前发布门禁

- 核心包：4 项 Node 单测。
- 游戏逻辑：132 项，包含请求生命周期/旧档迁移、关键语音资产、失败交易原子性、六章 24 节点通关和选择存档过滤。
- Android 资源同步：5 项，覆盖空目录启动、引擎运行时及开发文件排除。
- Android 构建配置：10 项，锁定官方源优先、镜像回退的解析顺序，以及 Web / Android v0.2 版本一致性。
- 游戏浏览器：5 条 E2E，覆盖安全区、爽玩按钮、任务架、剧情内教学、有声序章、渲染 bounds、失败交互和关键剧情演出。
- 下载站：桌面/移动两条 E2E。
- Android：单测、lint、debug/release 构建、APK 签名校验，再做目标真机安装和玩法冒烟。

## 修复位置

- 通用核心：`web/js/engine` Git submodule（`frost-merge-core`）。
- 业务状态：`web/js/core/GameState.js`。
- 商店/弹窗：`web/js/ui/UI.js`。
- Pixi 棋盘：`web/js/scenes/BoardScene.js`。
- 逻辑回归：`test/logic.test.js`。
- Android 资源同步回归：`test/sync-android.test.js`。
- Android 仓库顺序回归：`test/android-build-config.test.js`。
- 浏览器回归：`test/e2e.spec.js`。
