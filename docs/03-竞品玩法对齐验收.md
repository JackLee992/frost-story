# 《冰霜物语》v0.2 竞品玩法与叙事互动对齐验收

对标对象为 `Frost Valley™ Merge & Story` v1.8.20。结论基于已授权取得的 APK/XAPK 静态拆解：IL2CPP 元数据、YooAsset 目录和资源命名。目标是复刻其**通用核心玩法循环**，不复制受版权保护的美术、对白、角色或关卡表达。

## 核心循环结论

`点生成器 → 产出材料 → 两个同族同级物品拖拽合并 → 听村民说明并接受请求 → 凑齐材料 → 交付得金币/经验与角色回应 → 扩棋盘与升级 → 推进剧情重建`

v0.2 保留了已验证的首局循环，并把主线扩展为六章 24 节点。前三章的关键节点不再只是“交材料→看对白”，而是增加亲手点燃、区域点亮、仪式操作和三选一装修，再由角色对玩家刚才的动作做出回应。

## 逐项对齐矩阵

| 玩法 | 竞品拆解证据 | 《冰霜物语》v0.2 | 验收状态 |
|---|---|---|---|
| 合并棋盘 | `MergeGameBoard`、`BoardCells`、`BoardSpaceUnlockCount` | 6×8、初始 24 格、金币/等级逐格融化 | 已对齐核心行为 |
| Merge-2 | `OnMergeReduceAndAutoLevelUp`、拖拽相关符号 | 同族同级两个物品拖到一起升 1 级；异物换位 | 已对齐 |
| 生成器 | `GeneratorProduceLogic`、normal/lucky weight、冷却字段 | 点击耗体力、1~3 级权重产出、8 次后充能 | 已对齐 |
| 体力 | `session_energy_use`、energy multiplier、购买入口 | 生成消耗、90 秒恢复、升级回满、体力瓶 | 已对齐 v0.1 |
| 顶部订单 | `orderTray`、`submit_order`、`OrderSameChainDiffLevel` | 默认收进任务架；3 槽 NPC 先登场说明缘由，玩家接受后才可交付，完成后有角色回应 | 已对齐核心并增强叙事 |
| 等级与格子 | `level_up`、`level_gate`、board space 字段 | 经验升级、等级门槛、金币融化冰封格 | 已对齐 |
| 宝箱 | `MergeChestGacha`、跳过解锁字段 | 三档宝箱、倒计时、钻石跳过、批量掉落 | 已对齐基础箱 |
| 霜泡 | `bubblechess`、`lt_get_bubble`、`lt_buy_bubble` | 概率包裹、点破或 15 秒自动破 | 已对齐简化版 |
| 剧情重建 | `story_build`、chapter、story progress | 6 章 24 节点，材料+金币重建并触发原创演出 | 已对齐核心行为 |
| 场景内对白 | 成熟产品将角色放在当前修复地点，以短对白频繁回馈 | 单一电影舞台连续换句、左右站位、逐字、回顾与跳过 | 已实现可验收版 |
| 关键 CG | 公开商店图强调地点、动作和人物关系 | 满屏氛围层 + 完整 16:9 画幅，人物与关键动作不被竖屏裁掉 | 已实现并有安全区 E2E |
| 玩家参与 | 修复结果立即可见，部分地点提供装修选择 | 三类点击序列 + 村厅灯三选一，选择进存档、手记和后续对白 | 已实现前三章垂直切片 |
| 章节悬念 | 单一长期谜题串联日常任务 | 六章每章一个可见问题，每段回答小问题后抛出下一问 | 已数据化并受逻辑测试约束 |
| 有声演出 | BGM、环境层、操作音效与角色语音共同建立反馈层级 | 6 BGM + 风雪环境声 + 9 音效；序章、首次委托和首个 CG 有 12 段离线语音，台词期间自动 duck BGM | 已实现首章关键链 |
| 图鉴 | collection card / handbook 相关资源 | 六族八级曾获得记录与未解锁剪影 | 已对齐 |
| 内容热更 | YooAsset 的 Default/Story/Activity package | HTTPS 清单 + SHA-256 校验内容包，次启动切换 | 能力对齐，格式原创 |
| 新手教学 | `Onboarding`、`shop_tutorial_*`、`story_how_to_play_*` | 不显示步骤编号；用啾可剧情短句、目标光圈、点击/拖拽手势和冈特来访事件引导，规则页仅供主动查看 | 已实现 |

## v0.2 明确不冒充对齐的外围系统

- 仓库扩容、云存档、账号同步。
- 激励广告、插屏广告、IAP 和归因 SDK。
- Battle Pass、锦标赛、签到、每日/每周任务及大量限时活动。
- 服务端运营数值和竞品的 ONNX/DDA 动态难度模型。
- Spine 角色动画、换装以及剧情桥接小游戏。

这些内容不影响 v0.2 的合并主循环与六章试玩，但属于后续版本范围。当前的剧情互动集中在前三章，第 4–6 章仍需增加手势多样性、角色表情和真正改变场景图层的装修结果。完整商业化产品不能等同于本次垂直切片。

## 自动化验收映射

- `test/logic.test.js`：132 项，覆盖请求接受状态机、旧档迁移、NPC 剧情字段、12 段关键语音、确定性首局、六章 24 节点及损坏值过滤。
- `test/e2e.spec.js`：5 条，覆盖紧凑任务架/互斥展开与持久化、有声序章、剧情内教学、失败交易、连续电影对白、完整 CG、亲手复苏与装修选择闭环。
- `test/site.spec.js`：下载页在手机/桌面视口下的 APK 下载入口和动态版本信息。

每次发包前必须重新执行上述测试、Android Lint、APK 签名校验和至少一台 ARM64 真机安装启动测试。
